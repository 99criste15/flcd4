int main()
	{
		int a.
        int b.
        int c.
        a = readLine().
        b = readLine().
        c = readLine().
        if ( a >= b && a > c) {writeLine("is the biggest number ").}.
        if ( b >= a && b <= c) {writeLine("is the biggest number ").}.
        if ( c < a && c > b) {writeLine("is the biggest number ").}.
        return 0.

	}.
